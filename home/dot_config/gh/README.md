# GitHub CLI Configuration

This directory contains non-secret GitHub CLI preferences.

- Source: `home/dot_config/gh/config.yml`
- Target: `~/.config/gh/config.yml`

## Tracked vs Untracked

- Tracked: command aliases and global behavior in `config.yml`
- Not tracked: `hosts.yml` (auth/account state can be sensitive or machine-specific)
