# Atuin Configuration

This directory contains Atuin settings.

- Source: `home/dot_config/atuin/config.toml`
- Target: `~/.config/atuin/config.toml`

## Notes

- This config controls shell history search behavior and sync UX.
- Keep auth/session state out of git; only static preferences belong here.
